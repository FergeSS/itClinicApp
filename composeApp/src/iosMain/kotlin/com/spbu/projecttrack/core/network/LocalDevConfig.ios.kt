package com.spbu.projecttrack.core.network

/**
 * Конфигурация для локальной разработки (iOS)
 * 
 * На iOS симуляторе используется localhost
 * На реальном устройстве нужно вручную указать IP
 */
object LocalDevConfig {
    /**
     * Fallback IP адрес для реальных iOS устройств
     * TODO: Обновите этот IP на IP вашего компьютера
     * Узнать IP: ifconfig | grep "inet " | grep -v 127.0.0.1
     */
    private const val FALLBACK_IP = "192.168.1.100"  // TODO: Замените на ваш IP
    
    /**
     * Получить IP адрес компьютера
     * На iOS нет простого способа автоопределения IP хоста
     */
    fun getHostIP(): String {
        println("🍎 iOS: используется fallback IP: $FALLBACK_IP")
        return FALLBACK_IP
    }
    
    /**
     * Получить информацию о сети (заглушка для iOS)
     */
    fun getNetworkInfo(): String {
        return "iOS: автоопределение IP не поддерживается\nИспользуется: $FALLBACK_IP"
    }
}


